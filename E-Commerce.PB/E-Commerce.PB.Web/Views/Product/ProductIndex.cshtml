@model IEnumerable<E_Commerce.PB.Web.Models.ProdutoViewModel>

<br/>
<br/>
<div class="container p-4 border">
    <div class="row pb-2">
        <div class="col">
            <h1 class="text-primary">produtos List</h1>
        </div>
        <div class="col text-end pt-1">
            <a asp-action="produtoCreate" class="btn btn-outline-primary">
                <i class="fas fa-plus"></i>Create New produto
            </a>
        </div>
    </div>
    <table class="table">
        <thead>
            <tr>
                <th>
                    Name
                </th>
                <th>
                    Category
                </th>
                <th>
                    Price
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @item.Name
                    </td>
                    <td>
                        @item.CategoryName
                    </td>
                    <td>
                        @((item.Price).ToString("c"))
                    </td>
                    <td>
                       <a asp-action="produtoUpdate" class="btn btn-success" asp-route-id="@item.Id">
                           <i class="fas fa-edit"></i>
                       </a>
                       @if(User.IsInRole("Admin")){
                           <a asp-action="produtoDelete" class="btn btn-danger" asp-route-id="@item.Id">
                               <i class="fas fa-trash"></i>
                           </a>
                       }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>